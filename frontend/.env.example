# Backend API (Railway URL in production, e.g. https://alep-intranet-api.up.railway.app/api/v1)
VITE_API_URL=http://localhost:8000/api/v1

# Supabase (only the public anon key is safe to expose to the browser)
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=
